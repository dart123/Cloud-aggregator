2. При открытии какой-либо папки вызываем для данного облака ListFolder, удаляем текущие сигнатуры файлов из БД для 
данного облака и сохраняем новые, и показываем их пользователю
(открыли другую папку -> загружаем файлы для папки из облака -> удаляем существующие сигнатуры для этого облака из БД ->
добавляем только что загруженные сигнатуры в БД -> отображаем сигнатуры пользователю

Двойное нажатие -> смотрим на cloud_header над нажатой папкой -> вызываем соответствующую функцию для этого облака в 
ajax.js (аргумент функции path -> получаем current_folder (отдельная ф-ция ajax) для данного облака -> добавляем имя
папки к current_folder - для yandex и dropbox; для box делаем list_Folder для current_folder и находим в списке 
папку с этим названием и берем ее id)